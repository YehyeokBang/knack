# README 영/한 분리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 루트 README를 영문 메인(README.md) + 한글(README.ko.md)로 분리하고, plugins/knack/README.md를 영문화하며, lint 싱크 검사를 README.ko.md까지 확장한다.

**Architecture:** 문서 3개(루트 영문/루트 한글/플러그인 영문) + lint 1줄 확장 + patch 버전 범프(0.1.0 → 0.1.1). 검증은 `./scripts/lint-plugin.sh`가 담당 (pre-commit hook이 매 커밋마다 자동 실행). spec: `docs/superpowers/specs/2026-06-07-readme-i18n-design.md`

**Tech Stack:** Markdown, bash (lint 스크립트)

**전제:** `feature/readme-i18n` 브랜치에서 작업 (이미 생성됨, spec 커밋 31b9f61 존재). 작업 디렉토리는 레포 루트.

**Task 순서 제약:** Task 1(README.ko.md 생성)이 Task 3(lint 확장)보다 먼저여야 한다 — lint를 먼저 확장하면 README.ko.md가 없어서 pre-commit이 실패한다.

---

### Task 1: README.ko.md 생성 (한글 문서 이동)

**Files:**
- Create: `README.ko.md`

- [ ] **Step 1: 현행 README.md를 README.ko.md로 복사**

```bash
cp README.md README.ko.md
```

- [ ] **Step 2: README.ko.md 제목 바로 아래에 전환 링크 헤더 삽입**

`README.ko.md`의 1행 `# knack` 바로 다음(2행)이 빈 줄인데, 그 자리에 링크 헤더를 삽입한다. 결과적으로 파일 시작이 다음과 같아야 한다:

```markdown
# knack

[English](./README.md) | **한국어**

요령, 손에 익은 재주. 개인용 Claude Code 플러그인 마켓플레이스 —
매일 쓰는 워크플로 습관을 스킬로 만들어 관리·배포한다.
```

나머지 내용(설치/업데이트/제공 스킬/레포 구조/요구사항/문의)은 현행 그대로 유지. 단, "레포 구조" 트리의 마지막 줄 `└── README.md` 아래에 README.ko.md가 새로 생기지만, 트리는 Task 2의 영문 README와 함께 양쪽 모두 아래로 갱신한다:

```
├── CHANGELOG.md
├── CLAUDE.md                    # 버전 범프 규칙·롤백 절차·브랜치 정책
├── README.md                    # 영문 (메인)
└── README.ko.md                 # 한글 (이 파일)
```

- [ ] **Step 3: lint 실행으로 기존 검사 통과 확인**

Run: `./scripts/lint-plugin.sh`
Expected: `ALL CHECKS PASSED` (README.ko.md는 아직 검사 대상 아님)

- [ ] **Step 4: 커밋**

```bash
git add README.ko.md
git commit -m "docs: README.ko.md 생성 — 한글 README 이동 + 전환 링크 헤더"
```

---

### Task 2: 루트 README.md 영문 전문 번역으로 교체

**Files:**
- Modify: `README.md` (전체 교체)

- [ ] **Step 1: README.md를 아래 영문 전문으로 교체**

구조는 현행 한글과 1:1 대응 (spec의 "전문 번역" 결정). 전체 내용:

````markdown
# knack

**English** | [한국어](./README.ko.md)

A knack — a trick of the trade, a skill that lives in your hands.
A personal Claude Code plugin marketplace that turns everyday workflow
habits into skills, managed and shipped from one place.

## Installation

Run these two lines in Claude Code, in order:

```
/plugin marketplace add YehyeokBang/knack
/plugin install knack@knack
```

Once installed, invoke skills as `/knack:<skill-name>`.

## Updates (auto-update)

Third-party marketplaces have **auto-update OFF by default**. With it
turned on, a push is picked up automatically in the next session:

```
/plugin  →  in marketplace settings, turn on auto-update for knack
```

The update trigger is the `version` value in `plugin.json` — a push
without a version bump is never applied to installed copies.

## Skills

> See the [CHANGELOG](./CHANGELOG.md) for version history and [plugin.json](./plugins/knack/.claude-plugin/plugin.json) for the current version.
> All skills ship and update together as a single plugin.

| Command | Description | Docs |
|---------|-------------|------|
| `/knack:handoff` | Generates a zero re-explanation handoff prompt for the next session at session end — verifies that paths and commit hashes exist, prints a code block, and copies it to the clipboard | [SKILL.md](./plugins/knack/skills/handoff/SKILL.md) |
| `/knack:retune` | Reader-tailored document rewriting — rewrites for the target audience (PM / non-developer / other roles). Fixes meaningless numbering, vague English abbreviations, translationese, and unexplained jargon | [SKILL.md](./plugins/knack/skills/retune/SKILL.md) |

## Repository structure

```
knack/
├── .claude-plugin/
│   └── marketplace.json         # Marketplace catalog (version kept in sync with plugin.json)
├── plugins/
│   └── knack/                   # Plugin (name: "knack")
│       ├── .claude-plugin/
│       │   └── plugin.json      # version — ★ auto-update trigger
│       ├── skills/
│       │   ├── handoff/         # /knack:handoff
│       │   │   ├── SKILL.md
│       │   │   └── references/  # 3 handoff-type templates
│       │   └── retune/          # /knack:retune
│       │       └── SKILL.md
│       └── README.md            # Skill index
├── scripts/
│   └── lint-plugin.sh           # Version sync, README sync, 500-line checks
├── CHANGELOG.md
├── CLAUDE.md                    # Version bump rules, rollback procedure, branch policy
├── README.md                    # English (main)
└── README.ko.md                 # Korean
```

> To add a new skill, just add a directory under `skills/` — it is auto-registered as `/knack:<directory-name>`.

## Requirements

- macOS (the clipboard copy in `/knack:handoff` depends on `pbcopy` — cross-platform support is on the roadmap)

## Contact

- Bugs and feature requests: [knack Issues](https://github.com/YehyeokBang/knack/issues)
````

- [ ] **Step 2: README.ko.md의 "레포 구조" 트리도 같은 모양으로 갱신** (Task 1 Step 2에 명시한 한글 주석 버전 — `README.md  # 영문 (메인)`, `README.ko.md  # 한글 (이 파일)` 2줄이 트리 끝에 있어야 함)

- [ ] **Step 3: lint 실행**

Run: `./scripts/lint-plugin.sh`
Expected: `ALL CHECKS PASSED` — 영문 표에 `/knack:handoff`, `/knack:retune` 문자열이 있으므로 싱크 검사 통과

- [ ] **Step 4: 커밋**

```bash
git add README.md README.ko.md
git commit -m "docs: 루트 README.md 영문 전문 번역으로 교체"
```

---

### Task 3: lint 싱크 검사에 README.ko.md 추가

**Files:**
- Modify: `scripts/lint-plugin.sh:16`

- [ ] **Step 1: 16행의 검사 대상 루프에 README.ko.md 추가**

변경 전:

```bash
for README in README.md "$PLUGIN_DIR/README.md"; do
```

변경 후:

```bash
for README in README.md README.ko.md "$PLUGIN_DIR/README.md"; do
```

- [ ] **Step 2: lint 실행으로 확장된 검사 통과 확인**

Run: `./scripts/lint-plugin.sh`
Expected: `ALL CHECKS PASSED` — README.ko.md의 한글 스킬 표에 `/knack:handoff`, `/knack:retune` 문자열이 이미 있음

- [ ] **Step 3: 음성 테스트 — 검사가 실제로 동작하는지 확인** (일시적으로 README.ko.md의 `/knack:retune`을 `/knack:retunee`로 바꾸고 lint 실행)

Run: `sed -i '' 's|/knack:retune|/knack:retunee|g' README.ko.md && ./scripts/lint-plugin.sh; git checkout README.ko.md`
Expected: lint 출력에 `✘ README.ko.md: skill '/knack:retune' missing from table`과 `✘ README.ko.md: lists '/knack:retunee' but skills/retunee/ does not exist` 포함, `LINT FAILED`. 마지막 `git checkout`으로 원복.

- [ ] **Step 4: 원복 확인 후 커밋**

```bash
git diff --stat   # scripts/lint-plugin.sh 1개 파일만 변경이어야 함
git add scripts/lint-plugin.sh
git commit -m "chore: lint README 싱크 검사에 README.ko.md 추가"
```

---

### Task 4: plugins/knack/README.md 영문화

**Files:**
- Modify: `plugins/knack/README.md` (전체 교체)

- [ ] **Step 1: 아래 영문 전문으로 교체** (분리 없음, 단일 영문 — spec 결정)

````markdown
# knack plugin — skill index

The skills this plugin provides. For installation, updates, and other
marketplace-level guidance, see the [repository root README](../../README.md).

## Skills

| Command | Description | Docs |
|---------|-------------|------|
| `/knack:handoff` | Generates a zero re-explanation handoff prompt for the next session at session end (verification + clipboard copy) | [SKILL.md](./skills/handoff/SKILL.md) |
| `/knack:retune` | Reader-tailored document rewriting — rewrites for the target audience | [SKILL.md](./skills/retune/SKILL.md) |

See [`.claude-plugin/plugin.json`](./.claude-plugin/plugin.json) for the single plugin version.

## Directory layout

```
plugins/knack/
├── .claude-plugin/
│   └── plugin.json          # Version — auto-update trigger
├── skills/
│   ├── handoff/
│   │   ├── SKILL.md
│   │   └── references/      # execution / midwork / research templates
│   └── retune/
│       └── SKILL.md
└── README.md                # This file (index)
```

Each skill directory's `SKILL.md` defines the workflow the Claude agent follows.
````

- [ ] **Step 2: lint 실행**

Run: `./scripts/lint-plugin.sh`
Expected: `ALL CHECKS PASSED`

- [ ] **Step 3: 커밋** (feature 브랜치이므로 plugins/ 변경에 버전 범프 불필요 — pre-commit의 범프 거부는 main에서만 발동, CLAUDE.md 규칙)

```bash
git add plugins/knack/README.md
git commit -m "docs: plugins/knack/README.md 영문화"
```

---

### Task 5: 버전 범프 0.1.1 (squash 전 마지막 커밋)

**Files:**
- Modify: `plugins/knack/.claude-plugin/plugin.json:4`
- Modify: `.claude-plugin/marketplace.json:12`
- Modify: `CHANGELOG.md` (Unreleased 아래에 0.1.1 섹션 삽입)

- [ ] **Step 1: plugin.json 버전 변경**

`"version": "0.1.0"` → `"version": "0.1.1"` (4행)

- [ ] **Step 2: marketplace.json 버전 동기화**

`"version": "0.1.0"` → `"version": "0.1.1"` (12행, plugins[0])

- [ ] **Step 3: CHANGELOG.md에 0.1.1 항목 추가**

`## [0.1.0] - 2026-06-07` 바로 위에 삽입:

```markdown
## [0.1.1] - 2026-06-07

### Changed

- README 영/한 분리: 루트 README.md를 영문 메인으로 전환, 한글은 README.ko.md로
  이동 (상단 English | 한국어 전환 링크). plugins/knack/README.md 영문화.
- lint-plugin.sh: README 싱크 검사 대상에 README.ko.md 추가.
```

- [ ] **Step 4: lint 실행으로 버전 동기화 확인**

Run: `./scripts/lint-plugin.sh`
Expected: `✓ version sync: 0.1.1` 포함, `ALL CHECKS PASSED`

- [ ] **Step 5: 커밋**

```bash
git add plugins/knack/.claude-plugin/plugin.json .claude-plugin/marketplace.json CHANGELOG.md
git commit -m "chore: 버전 0.1.1 — README 영/한 분리"
```

---

### Task 6: main으로 squash merge

**Files:** (없음 — git 작업만)

- [ ] **Step 1: main에서 squash merge**

```bash
git checkout main
git merge --squash feature/readme-i18n
git commit -m "docs: README 영/한 분리 (v0.1.1)

- 루트 README.md 영문 메인 전환, 한글은 README.ko.md로 이동 (전환 링크 헤더)
- plugins/knack/README.md 영문화
- lint README 싱크 검사에 README.ko.md 추가
- 버전 0.1.1 (patch)"
```

Expected: pre-commit이 main에서 "plugins/ 변경 + 버전 변경됨"을 확인하고 통과 (plugin.json diff에 version 변경 포함)

- [ ] **Step 2: 최종 lint 확인**

Run: `./scripts/lint-plugin.sh`
Expected: `ALL CHECKS PASSED`

- [ ] **Step 3: push 전 사용자 확인** — push는 auto-update 배포를 트리거하므로 (CLAUDE.md) 사용자에게 push 여부를 확인받은 뒤:

```bash
git push origin main
git branch -d feature/readme-i18n
```
